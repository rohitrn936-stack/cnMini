
#TO RUN or execute the file you'll need to make sure that both the system (client and the server are on the same wifi connection like a under same lan or pan )
#in client file instead of local host mention the ipv4 address of the system in which you'll be running the server program ,and in the server program swap local host with 0.0.0.0
#run the server file first then the clien file in different terminals 
#currently the number of clients that can be connected is 2 ,that is left static but that can be done dynamic by taking a simpel input on the server side 
#the password for the client connection as well is static even that can be changed by doing it dynamically 
#default password is 1234 username can be anything
